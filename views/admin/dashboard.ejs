<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@2.2.0/fonts/remixicon.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            overflow-x: hidden;
            background: linear-gradient(300deg, #c3bcc6, #b9e6e3, rgb(255, 255, 255));
            background-size: 180% 180%;
            animation: gradient-animation 6s ease infinite;
            /* This will hide horizontal scrollbar */
        }

        .img {
            width: 50px;
            height: 50px;
            border-radius: 100px;
        }

        @keyframes gradient-animation {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        header {
            background-color: #323f97;
            color: #ffffff;
            padding: 0 0 0 25px;
            text-align: left;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        header img {
            width: 30rem;
            margin: 20px;
        }

        header h1 {
            margin: 0;
        }

        nav {
            display: flex;
            align-items: center;
            margin-right: 10rem;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            font-size: 1.3rem;
            margin: 0 25px;
        }

        nav a:hover {
            color: chartreuse;
        }

        .toggle-btn {
            display: none;
            /* Initially hide the toggle button */
            cursor: pointer;
            font-size: 28px;
            position: absolute;
            top: 25px;
            right: 15px;
        }

        footer {
            background-color: #323f97;
            color: #fff;
            padding: 10px;
            position: fixed;
            bottom: 0;
            width: 100%;
            height: 3rem;
            text-align: center;
        }

        /* Responsive styles */
        @media (max-width: 1100px) {
            header img {
                width: 20rem;
            }

            nav a {
                font-size: 1rem;
                margin: 0 15px;
            }
        }

        @media (max-width: 768px) {
            header img {
                width: 18rem;
            }

            .toggle-btn {
                display: block;
                /* Display the toggle button on small screens */
            }

            nav {
                margin-right: 0rem;
                display: flex;
                flex-direction: column;
                /* Display the navigation links vertically */
                align-items: center;
                margin-top: 7px;
                display: none;
                /* Initially hide the navigation links on small screens */
                position: absolute;
                top: 50px;
                right: 0px;
                background-color: #323f97;
                padding: 60px;
                animation: slideInOpacity 0.5s ease-in-out;
            }

            @keyframes slideInOpacity {
                from {
                    opacity: 0.5;
                    transform: translateX(100%);
                }

                to {
                    opacity: 1;
                    transform: translateX(0);
                }
            }

            nav a {
                color: #fff;
                text-decoration: none;
                margin: 5px 0;
            }

        }

        .img {
            width: 50px;
        }
    </style>
    <style> 
    
        .button {
            padding: 8px 12px;
            text-decoration: none;
            background-color: transparent;
            color: #fff;
            border: 1px solid rgb(134, 129, 129);
            border-radius: 4px; 
            cursor: pointer; 
        }
    
        .button:hover {
            background-color: #94a19f;
        }

        .delete{
            width: 20px;
        }
    </style>
    
    <style>
        /* ... (existing styles) */

        .container {
            max-width: 800px;
            margin: 40px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .container h1 {
            color: #323f97;
            text-align: center;
        }

        table {
            width: 100%;
            margin-top: 20px;
            border-collapse: collapse;
        }

        table,
        th,
        td {
            border: 1px solid #ddd;
        }

        th,
        td {
            padding: 12px;
            text-align: left;
        }

        th {
            background-color: #323f97;
            color: #fff;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        tr:hover {
            background-color: #f1f1f1;
        }
    </style>

<style> 

    .container {
        max-width: 60%;
        margin: 40px auto;
        padding: 20px;
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        transition: box-shadow 0.3s ease-in-out;
    }

    .container h1 {
        color: #323f97;
        text-align: center;
        margin-bottom: 20px;
    }

    table {
        width: 100%;
        margin-top: 20px;
        border-collapse: collapse;
        overflow: hidden;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        border-radius: 8px;
    }

    th, td {
        border: 1px solid #ddd;
        padding: 15px;
        text-align: left;
    }

    th {
        background-color: #323f97;
        color: #fff;
        font-weight: bold;
    }

    tr:nth-child(even) {
        background-color: #f9f9f9;
    }

    tr:hover {
        background-color: #f1f1f1;
    }

    /* Add some animation to the table rows for a smoother hover effect */
    tr {
        transition: background-color 0.3s ease;
    }

    /* Add a subtle border radius to the table cells */
    th, td {
        border-radius: 4px;
    }

    .userimg{
        width: 100%;
        height: 15vh;
        /* height: 100px; */
    }
</style>

</head>

<body>

    <header>
        <div>
            <img src="/userimages/car.png" alt="Icon">
        </div>
        <div class="toggle-btn" onclick="toggleNav()">☰</div>
        <nav>
            <a href="/admin/home">Home</a>
            <a href="/admin/home">BookCar</a> 
            <a href="/aboutpage">About</a>
            <a href="/admin/home"><img class="img" src="/userimages/<%= admin.image  %>" alt=""></a>
        </nav>
    </header>

    <div class="container">
        <h1>Admin Dashboard</h1>

        <table width="50%">
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Mobile</th>
                <th>Image</th>
                <th>Verified</th>
                <th>Modify</th>
            </tr>
            <% if(users.length> 0){
                for(let i=0;i<users.length;i++){ %>
                    <tr>
                        <td>
                            <%= users[i].name %>
                        </td>
                        <td>
                            <i style="color: #323f97;" ><%= users[i].email %></i>
                        </td>
                        <td>
                            +91 <%= users[i].mobile %>
                        </td>
                        <td> <img class="userimg" src="/userimages/<%= users[i].image %>" alt="" /> </td>
                        <td>
                            <% if(users[i].is_varified==0){ %>
                                <p>Not Verified</p>
                                <% } else{ %>
                                    <p>Verified</p>
                                    <% } %>
                        </td>

                        <td>
                            <a class="button" href="/admin/edit-user?id=<%= users[i]._id %>"><svg class="delete" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M9.24264 18.9964H21V20.9964H3V16.7538L12.8995 6.85431L17.1421 11.0969L9.24264 18.9964ZM14.3137 5.44009L16.435 3.31877C16.8256 2.92825 17.4587 2.92825 17.8492 3.31877L20.6777 6.1472C21.0682 6.53772 21.0682 7.17089 20.6777 7.56141L18.5563 9.68273L14.3137 5.44009Z" fill="rgba(7,98,243,1)"></path></svg></a>
                            <br />
                            <br />
                            <a class="button" href="/admin/delete-user?id=<%= users[i]._id %>"><svg class="delete" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M7 4V2H17V4H22V6H20V21C20 21.5523 19.5523 22 19 22H5C4.44772 22 4 21.5523 4 21V6H2V4H7ZM6 6V20H18V6H6ZM9 9H11V17H9V9ZM13 9H15V17H13V9Z" fill="rgba(250,7,7,1)"></path></svg></a>
                        </td>


                    </tr>
                    <% } } else{ %>
                        <tr>
                            <td colspan="5"> User Not Found </td>
                        </tr>
                        <% } %>

        </table>
    </div>

    <footer>
        &copy; 2023 Your Website Name. All rights reserved.
    </footer>

    <script>
        function toggleNav() {
            var nav = document.querySelector('nav');
            var toggleBtn = document.querySelector('.toggle-btn');

            if (nav.style.display === 'flex' || nav.style.display === '') {
                nav.style.display = 'none';
                toggleBtn.innerHTML = '☰';
            } else {
                nav.style.display = 'flex';
                toggleBtn.innerHTML = '✖';
            }
        }
    </script>

</body>

</html>