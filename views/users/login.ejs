<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            overflow-x: hidden;
            background: linear-gradient(300deg, #c3bcc6, #b9e6e3, rgb(255, 255, 255));
            background-size: 180% 180%;
            animation: gradient-animation 6s ease infinite;
            /* This will hide horizontal scrollbar */
        }

        @keyframes gradient-animation {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        header {
            background-color: #323f97;
            color: #ffffff;
            padding: 0 0 0 25px;
            text-align: left;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        header img {
            width: 30rem;
            margin: 20px;
        }

        header h1 {
            margin: 0;
        }

        nav {
            display: flex;
            align-items: center;
            margin-right: 10rem;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            font-size: 1.3rem;
            margin: 0 25px;
        }

        nav a:hover {
            color: chartreuse;
        }

        .toggle-btn {
            display: none;
            /* Initially hide the toggle button */
            cursor: pointer;
            font-size: 28px;
            position: absolute;
            top: 25px;
            right: 15px;
        }

        footer {
            background-color: #323f97;
            color: #fff;
            padding: 10px;
            position: fixed;
            bottom: 0;
            width: 100%;
            height: 3rem;
            text-align: center;
        }

        /* Responsive styles */
        @media (max-width: 1100px) {
            header img {
                width: 20rem;
            }

            nav a {
                font-size: 1rem;
                margin: 0 15px;
            }
        }

        @media (max-width: 768px) {
            header img {
                width: 18rem;
            }

            .toggle-btn {
                display: block;
                /* Display the toggle button on small screens */
            }

            nav {
                margin-right: 0rem;
                display: flex;
                flex-direction: column;
                /* Display the navigation links vertically */
                align-items: center;
                margin-top: 7px;
                display: none;
                /* Initially hide the navigation links on small screens */
                position: absolute;
                top: 50px;
                right: 0px;
                background-color: #323f97;
                padding: 60px;
                animation: slideInOpacity 0.5s ease-in-out;
            }

            @keyframes slideInOpacity {
                from {
                    opacity: 0.5;
                    transform: translateX(100%);
                }

                to {
                    opacity: 1;
                    transform: translateX(0);
                }
            }

            nav a {
                color: #fff;
                text-decoration: none;
                margin: 5px 0;
            }

        }

        .img {
            width: 50px;
        }
    </style>

    <style>
        .user-section {
            padding: 50px 0;
        }

        .container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            max-width: 900px;
            margin: auto;
        }

        .container h1 {
            color: #323f97;
            margin-bottom: 20px;
            font-family: "Arvo", cursive;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        form input {
            margin-bottom: 0px;
            padding: 10px;
            border: 1px solid #1f2b73;
            border-radius: 15px;
            background-color: transparent;
        }

        form input[type="submit"] {
            background-color: #323f97;
            color: #fff;
            cursor: pointer;
        }

        form input[type="submit"]:hover {
            background-color: #1228a4;
        }


        .submit {
            width: 10rem;
        }

        .div1 {
            background-color: transparent;
            display: flex;
            flex-direction: column;
            align-items: center;
            border: 1px solid red;
            border-radius: 50px;
            box-shadow: 0 0 10px rgba(255, 149, 0, 0.3);
            margin-top: 5rem;
        }

        .message {
            color: red;
        }

        .btn:hover {
            color: #0026fb;
        }

        .div2 {
            /* border: 1px solid #1228a4; */
            background-color: transparent;
            max-width: 110%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .img2 {
            width: 500px;
        }

        .arrow {
            width: 180px;
        }

        @media (max-width: 768px) {
            .container {
                grid-template-columns: 1fr;
            }

            .div1 {
                max-width: 90%;
                margin-left: 20px;
            }

            .img2 {
                width: 380px;
                /* display: none; */
            }

            .arrow {
                display: none;
            }
        }
    </style>
</head>

<body>

    <header>
        <div>
            <img src="userimages/car.png" alt="Icon">
        </div>
        <div class="toggle-btn" onclick="toggleNav()">☰</div>
        <nav>
            <a href="/">Home</a>
            <a href="about">About</a>
            <a href="login">Login</a>
            <a href="register">Register</a>
            <a href="/admin"><img class="img" src="userimages/admin_login.png" alt=""></a>
        </nav>
    </header>

    <section class="user-section">
        <div class="container">
            <div class="div1">
                <h1>User Login!!</h1>
                <div class="message">
                    <% if(typeof message !=='undefined' ){ %>
                        <h4>
                            <%= message %>
                        </h4>
                        <% } %>
                </div>

                <form action="/login" method="post">
                    <input type="email" name="email" placeholder="Enter email" required id="">
                    <br>
                    <input type="password" name="password" required placeholder="Enter password" id="">
                    <br>
                    <input type="submit" value="Login">
                </form> <br>
                <a class="btn" href="/forget">Forget Password</a> <br>
                <a class="btn" href="/verification">Verification</a> <br>
            </div>
            <div class="div2">
                <img class="img2" src="userimages/paisa.png" alt="">
                <img class="arrow" src="userimages/arow.gif" alt="">
            </div>
        </div>
    </section>

    <footer>
        &copy; 2023 Your Website Name. All rights reserved.
    </footer>

    <script>
        function toggleNav() {
            var nav = document.querySelector('nav');
            var toggleBtn = document.querySelector('.toggle-btn');

            if (nav.style.display === 'flex' || nav.style.display === '') {
                nav.style.display = 'none';
                toggleBtn.innerHTML = '☰';
            } else {
                nav.style.display = 'flex';
                toggleBtn.innerHTML = '✖';
            }
        }
    </script>

</body>

</html>