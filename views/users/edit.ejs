<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            overflow-x: hidden;
            background: linear-gradient(300deg, #b7a4bf, #c1d3d2, rgb(115, 132, 141));
            background-size: 180% 180%;
            animation: gradient-animation 6s ease infinite;
            /* This will hide horizontal scrollbar */
        }

        @keyframes gradient-animation {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        header {
            background-color: #323f97;
            color: #ffffff;
            padding: 0 0 0 25px;
            text-align: left;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        header img {
            width: 30rem;
            margin: 20px;
        }

        header h1 {
            margin: 0;
        }

        nav {
            display: flex;
            align-items: center;
            margin-right: 10rem;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            font-size: 1.3rem;
            margin: 0 25px;
        }

        nav a:hover {
            color: chartreuse;
        }

        .toggle-btn {
            display: none;
            /* Initially hide the toggle button */
            cursor: pointer;
            font-size: 28px;
            position: absolute;
            top: 25px;
            right: 15px;
        }

        footer {
            background-color: #323f97;
            color: #fff;
            padding: 10px;
            position: fixed;
            bottom: 0;
            width: 100%;
            height: 3rem;
            text-align: center;
        }

        /* Responsive styles */
        @media (max-width: 1100px) {
            header img {
                width: 20rem;
            }

            nav a {
                font-size: 1rem;
                margin: 0 15px;
            }
        }

        @media (max-width: 768px) {
            header img {
                width: 18rem;
            }

            .toggle-btn {
                display: block;
                /* Display the toggle button on small screens */
            }

            nav {
                margin-right: 0rem;
                display: flex;
                flex-direction: column;
                /* Display the navigation links vertically */
                align-items: center;
                margin-top: 7px;
                display: none;
                /* Initially hide the navigation links on small screens */
                position: absolute;
                top: 50px;
                right: 0px;
                background-color: #323f97;
                padding: 60px;
                animation: slideInOpacity 0.5s ease-in-out;
            }

            @keyframes slideInOpacity {
                from {
                    opacity: 0.5;
                    transform: translateX(100%);
                }

                to {
                    opacity: 1;
                    transform: translateX(0);
                }
            }

            nav a {
                color: #fff;
                text-decoration: none;
                margin: 5px 0;
            }

        } 
        .img {
            width: 50px;
            height: 50px;
            border-radius: 100px;
        }
    </style>
    <style>
        .container{ 
            margin: 0px 0 0 540px;
            max-width: 30%;
            display: flex;
            align-items: center;
            justify-content: center;
            /* border: 1px solid red; */
            border-radius: 50px;
            box-shadow: 0 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .con2{
            padding: 30px 0 30px 0;
        }
        .user{
            width: 150px;
            height: 150px;
            border-radius: 300px; 
            box-shadow: 0px 0px 20px 2px rgb(33, 32, 32);
        }
        form {
            display: flex;
            flex-direction: column;
        }

        form input {
            margin-bottom: 0px;
            padding: 7px;
            border: 1px solid #1f2b73;
            border-radius: 10px;
            background-color: transparent;
        }

        form input[type="submit"] {
            background-color: #323f97;
            color: #fff;
            cursor: pointer;
        }

        form input[type="submit"]:hover {
            background-color: #1228a4;
        }


        .submit {
            width: 10rem;
        }

        h1{
            text-align: center;
            font-family: "Arvo", cursive;
            color: #323f97;
        }
    </style>
</head>

<body>

    <header>
        <div>
            <img src="userimages/car.png" alt="Icon">
        </div>
        <div class="toggle-btn" onclick="toggleNav()">☰</div>
        <nav>
            <a href="/home">Home</a>
            <a href="/home">BookCar</a> 
            <a href="aboutpage">About</a>
            <a href="/home"><img class="img" src="userimages/<%= user.image %>" alt="<%= user.image %>"></a>
        </nav>
    </header>

    <section>
        <h1>You can edit your profile here...!</h1>
        <div class="container">
            <div class="con2" >
                <img class="user" src="userimages/<%= user.image  %>" alt="Profile Image">
    
                <form action="" enctype="multipart/form-data" method="post">
                    <!-- // enctype="multipart/form-data" ----=> To edit -->
    
                    <input type="file" name="image" id="">
    
                    <input type="hidden" name="user_id" value="<%= user._id  %>"> <br>
    
                    <input type="text" name="name" value="<%= user.name  %>" placeholder="Enter Name" required id="">
                    <br>
                    <input type="email" name="email" value="<%= user.email  %>" placeholder="Enter Email" required id="">
                    <br>
                    <input type="text" name="mno" value="<%= user.mobile  %>" placeholder="Enter Mobile" required id="">
    
                    <br>
                    <input type="submit" value="Update Profile">
    
                </form>
            </div>
        </div>
    </section>

    <footer>
        &copy; 2023 Your Website Name. All rights reserved.
    </footer>

    <script>
        function toggleNav() {
            var nav = document.querySelector('nav');
            var toggleBtn = document.querySelector('.toggle-btn');

            if (nav.style.display === 'flex' || nav.style.display === '') {
                nav.style.display = 'none';
                toggleBtn.innerHTML = '☰';
            } else {
                nav.style.display = 'flex';
                toggleBtn.innerHTML = '✖';
            }
        }
    </script>

</body>

</html>